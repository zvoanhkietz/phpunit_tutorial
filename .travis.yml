language: php

php:
  - 7.0

sudo: false

before_install:
  - composer self-update

install:
  - composer install --no-interaction --prefer-source

script:
  - docker exec -it phpenv /bin/bash
  - phpenv --version
  - phpenv install php-7.0.11
  - php -v
  - ./vendor/bin/phpunit
